/// Represents the input mode for entering values in the Sudoku grid.
enum InputMode {
  /// Digit input mode (for placing real values).
  digit,

  /// Pencil input mode (for placing notes/candidates).
  pencil,
}
